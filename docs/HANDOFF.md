# 현재 상태와 인수인계

갱신: 2026-09-20. 작업 절차는 [AGENTS](../AGENTS.md), 실행은 [README](../README.md),
구조는 [ARCHITECTURE](ARCHITECTURE.md), 음성 규칙은 [QUALITY](QUALITY.md)를 따른다.

## 촬영 이관 (2026-09-18)

화면 촬영과 자막이 `udemy-agent`에서 이 저장소로 왔다. 덱에서 `course-media.mjs`·
`narration.mjs`·`captions.mjs`·`capture*.mjs`와 `narration:*` npm 스크립트를 걷어냈고,
자막을 쓰던 레거시 TTS 경로(macOS 음성·mock·ElevenLabs)도 함께 정리했다. 덱은 대본·
화면·소스 고정을 소유한다. 근거는 [DECISIONS](DECISIONS.md).

확인한 것: 자막 cue가 실제 타임라인 13건·1645개에서 srt/vtt/json까지 이전과 동일,
세 화질 합성 촬영이 덱 원본과 프레임 수·전환 시각·출력 스트림 규격 일치, 실제 CH00
화면 30초를 1440p 자막 합성으로 촬영(750프레임, 정지 0초), 소스 판본 불일치 차단 동작,
`npm run check` 통과(앱 307·Python 298).

확인하지 않은 것: 실제 강의 한 편 전체나 4K 장시간 촬영은 다시 돌리지 않았다.
`playwright@1.62.0` 고정 이유와 브라우저를 올릴 때 다시 재는 절차는
[VIDEO-QUALITY](VIDEO-QUALITY.md)에 있다.

## 화면 녹화 (2026-09-18)

IBM BoB Level 3 제출 영상용 **디스플레이 수동 녹화**를 "화면 녹화" 메뉴로 만들었다.
구조는 [ARCHITECTURE](ARCHITECTURE.md#화면-녹화), 규격·측정은
[VIDEO-QUALITY](VIDEO-QUALITY.md#화면-녹화), 결정 근거는
[SCREEN-CAPTURE-DESIGN](SCREEN-CAPTURE-DESIGN.md)에 있다.

확인한 것: 실제 DELL 10초 녹화를 앱과 같은 작업자·신호로 정지해 영상·음성 10.600초,
dup/drop 0. 취소 시 파일이 남지 않음. 실제 Electron 창에서 화면 목록 → 녹화 → 정지 →
완료 화면까지 동작. 녹화 → 기존 목소리 후보로 구간 교체 → 덱 1080p와 합치기가 복사로
끝남. `doctor`의 화면 기록 판정. `npm run check` 통과(앱 333·Python 298).

확인하지 않은 것: 실제 Bob 조작 15분 녹화와 그 영상의 시청 확인, 새 목소리 만들기부터
이어지는 전 과정(구간 교체에는 9월 17일 목소리 후보를 썼다), 1440p 모니터 녹화.
구간 음성 교체는 한 번에 120초까지라 긴 녹화의 내레이션은 여러 번 나눠 넣는다.
나눌 때마다 영상은 복사되지만 음성 트랙은 AAC로 다시 압축된다.

**결정 필요:** 음성 포함 녹화는 이 Mac에서 쓸 수 없다. ffmpeg가 마이크 소리의 약 13%를
놓쳐 검증에서 걸리고, 그 녹화에 내레이션을 넣으면 최대 0.7초 밀린다. 토글은 설계대로
두되 켜면 경고를 보이고 결과는 검증 실패로 남는다. 고치려면 설계에서 뺀 네이티브 녹음
또는 앱 창의 브라우저 녹음 API가 필요하다. 기본인 무음 녹화에는 영향이 없다.

## 앱 데모 촬영 (2026-09-20)

직접 만든 앱을 자동으로 조작하며 찍어 소개 영상을 만드는 경로를 만들었다. CLI로만 돌고
앱 화면 메뉴는 아직 없다. 실행법은 [README](../README.md#앱-데모-촬영-cli), 구조는
[ARCHITECTURE](ARCHITECTURE.md#앱-데모-촬영), 규격·측정은
[VIDEO-QUALITY](VIDEO-QUALITY.md#앱-데모-촬영), 결정 근거는
[APP-DEMO-DESIGN](APP-DEMO-DESIGN.md)에 있다.

확인한 것: 우리 Electron 44 + playwright 1.62 `_electron`으로 픽스처 앱을 띄워 타이핑 →
승인 대기 → 클릭 → 완료 대기를 실제로 찍고(장면 시각·좌표·화면 글, 무손실 gbrp 원본,
`finish({ endAt })`가 상한이 아닌 실제 끝 시각에서 끊음), 그 촬영을 렌더해 프레임 수 =
계획 길이·음성이 계획한 자리·자막·검증 보고서까지 확인했다. 커서와 클릭 지점 확대가
완성 영상에 실제로 찍혔다(`output/reviews/2026-09-20/app-demo-build/fixture/`).
`demo voice`는 실제 제작 목소리(Qwen3-TTS + `jaeho-ko-r16-v1` 0.60)로 한 후보를 만들어
그 음성으로 렌더까지 돌렸다. `zoompan`의 정수 좌표 떨림은 4K 입력에서 실제 값을 역산해
쟀고 설계 움직임에서는 묻힌다고 판단했다. `npm run check` 통과(앱 366·Python 298).

RICE 파일럿을 두 번 돌렸다. 시나리오를 고치지 않고 `config/demo/rice-core-flow.json`
그대로 기동·타이핑·승인 대기·클릭·완료까지 갔고, Ollama가 실제로 파일을 만들었다.
첫 촬영에서 **커서가 엉뚱한 자리에 찍히는 결함**을 찾아 고쳤다(창 위의 실제 마우스가
오버레이를 끌고 갔다. 지금은 우리가 보낸 좌표로 그리고 창은 `setIgnoreMouseEvents`로
둔다). 회귀 검사를 넣었고 두 번째 촬영에서 제자리를 확인했다.

결과는 `output/edits/2026-09-20/rice-core-flow/`다. 55.0초 촬영 → **21.9초 완성본**이며
1440p·4K 두 벌이다. 대본·목소리까지 전 과정을 한 번 돌렸다: 장면 3개에 후보 3벌씩
실제 제작 목소리로 만들고, 1번 후보로 렌더해 프레임 수 = 계획 길이·음성 길이 = 영상
길이를 확인했다. 내레이션이 계획한 자리(0.3s·4.5s·16.7s)에서 실제로 들리는지 음량으로
확인했고 자막도 나왔다. 결과 폴더의 `review.html`이 영상·구간·확대·대본·후보 음성을
한 쪽에 모은다.

첫 완성본에서 사용자가 **RICE가 "알아이씨이"로 읽힌다**고 지적해 고쳤다. inline 항목은
영어 음성으로 가지 않고 한국어 모델이 철자를 직접 읽는데, 대문자 약어라 철자로 읽혔다.
철자를 재서(`probe_term_pronunciation.py`) 자막 원문은 `RICE`, 발음문은 `Rice`로 나눴고
다시 만든 후보 3벌이 모두 `라이스`로 읽혔다(거리 0.000). 자막이 어디 있냐는 지적으로
검수 화면에 자막을 덱과 같은 모양으로 겹쳐 그린다 — 영상에 굽지는 않는다.

`ask` 장면에는 영어 목소리로 읽은 표본 둘을 후보로 함께 넣어 두었다(제작 경로가 아닌
청취 실험이다). 고르면 `speech_segments` 규칙을 바꿔야 한다.

확인하지 않은 것: **사용자 시청과 청취 선택이 남았다.** 대본은 Claude 초안을 그대로
썼고 목소리는 1번 후보를 임의로 골랐다. 빨리 감기 배율(4배)·출력 해상도·확대 세기가
그 판단이다. 자막을 영상에 굽는 길은 만들지 않았다 — 이 맥의 ffmpeg에는 libass도
drawtext도 없어 브라우저로 자막 그림을 만들어 겹치는 경로가 필요하다.
`text_candidate` 경로는 `voiceRouting`을 후보 기록에 남기지 않는다(강의 경로는 남긴다).
RICE 저장소 수정은 사용자 승인 후에 한다.

**다음:** 사용자 시청·청취 → 고른 후보를 `voice.selected`에 적고 `render` 다시.
대본을 고치면 그 장면만 `voice`를 다시 돌린다.
[APP-DEMO-DESIGN](APP-DEMO-DESIGN.md#6-남은-결정)의 남은 결정을 그 영상으로 닫는다.

## 제작 상태

- 한국어는 Qwen3-TTS + `jaeho-ko-r16-v1` LoRA 0.60, 영어 인용문은
  `english-speaker-only-v1`이다. 설치·A/B·학습은 완료됐다.
- 미등록 용어도 제작하고 완성 후 검수한다. `B-3102 → 비 삼일공이`,
  `R-042 → 알 공사이`는 사전에 등록돼 있다. CH03의 335·359·389페이지에 적용된다.
- 레슨 분할 제작은 개별 실패 후 나머지를 계속한다. 실패한 레슨의 이어하기를 위해
  고정 입력과 진행 기록을 보존하며, 새 제작의 캐시 금지는 유지한다.
- 남은 시간은 단계별 실측으로 내며 촬영 중에도 답을 낸다. 아직 잴 것이 없으면
  계산 중 표시를 낸다. 일부 실패와 전체 실패를 구분하며 완료·실패·중지 후에는
  타이머를 종료한다. 앱 코드 변경은 재시작 후 반영된다.
- 다듬기에서 재생성으로 풀리지 않는 읽기는 읽을 말을 직접 적어 후보를 만든다.
  자막·대본은 그대로 두고 발음문만 바꾸며, 자동 판독 대신 사람이 듣고 고른다.
  확인할 곳은 페이지로 묶어 접고, 같은 작업의 앞뒤 편은 머리글에서 바로 연다.
  같은 낱말이 여러 페이지에서 걸리면 목록이 그 사실을 함께 알린다.

## 수정본 찾기

| 대상 | 경로·주의점 |
| --- | --- |
| CH01 전체·CH02 16편 | `output/edits/2026-09-10/ch01-ch02-english-retrofit-index/최신 영상 목록.md` |
| CH02 L04 후속 수정본 | `output/edits/2026-09-10/ch02-l04-anthropic-refined/CH02 L04 · 그래서 어떻게 만들라는 건가 - 영어 보정 v2.mp4` |
| CH00 시작음 제거본 | `output/edits/2026-09-08/ch00-restart-repair-172747/` |

CH02 L04의 193.295–203.055초는 사용자가 고른 `앤쓰로픽` 문장 후보다. 해당
발음문에만 적용했고 전역 사전은 바꾸지 않았다. 준비·청취 기록은
`output/reviews/2026-09-10/ch02-l04-anthropic-followup/`에 있다.
CH01·CH02 전체 영어 보정 근거는 `output/reviews/2026-09-10/ch01-ch02-english-retrofit/`다.
CH03 L02의 `한 끗이 → 한 끄시`는 사전에만 반영했고 당시 영상은 재제작하지 않았다.

후속 편집은 최신 수정본의 PCM·타임라인·자막을 사용한다. 원래 manifest 음성으로
돌아가 이미 고친 부분을 덮지 않는다. 개별 샘플 승인과 전체 영상·연결부 승인은 별도다.

## 남은 검증 범위

- CH03 발음·실패 처리 변경은 자동 검사와 현재 대본 처리로 확인했다. 해당 변경으로
  실제 CH03 전체 영상을 다시 생성하거나 청취 승인받은 것은 아니다.
- 실제 Electron 창과 긴 4K 강의의 시청 검증은 코드·모델 대체물 검사로 대신하지 않는다.
  짧은 촬영·인코딩 측정의 조건과 한계는 [VIDEO-QUALITY](VIDEO-QUALITY.md)에 있다.
- 자동 검수는 영어 억양·강세·음색을 승인하지 않는다. 실제 MLX 배열 접근·새 합성의
  성공도 테스트 통과나 `metal.is_available()`만으로 주장하지 않는다.

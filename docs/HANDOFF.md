# 프로젝트 인수인계

> 작성일: 2026-08-25
> 상태: 로컬 제작 앱 고도화 완료 — 텍스트 다중 후보, 경로 설정, 자산 이관, 영상 캐시 우회 적용

## 현재 작업을 시작할 때

아래 날짜별 절은 판단 이력이다. 과거의 “다음 작업”·A/B·승인 대기를 현행 지시로
실행하지 않는다. 음성 기본값은 AGENTS, 제작 동작은 README와 ARCHITECTURE의 현행 경로를 따른다.

2026-09-06 단어 내부 끊김 검사 v3를 강의 제작·페이지 교체에 적용했다.
Whisper 단어 시각, Qwen 강제 정렬, 실제 발화 구간을 함께 확인하고 근거가 있는
후보만 재생성한다. 음성 프로필 0.60과 기존 발음 관문은 유지한다. 일반 텍스트
후보 생성은 별도 경로다. 세부 판정 계약은 ARCHITECTURE의 자동 발음 검수 절을 따른다.

실모델 검증 결과는 `artifacts/validation/prosody/20260906-143420-547d9d/`다.
24.64초의 한·영 샘플 모두 첫 후보에서 통과했고, 재시도 0회, 순수 생성 16.25초,
최대 프로세스 Metal 메모리 기록 11.361GB였다. 일반 VS Code 터미널에서 실행했다.
이 과정에서 보호된 영어 문장 안의 `Bots → 봇츠`까지 요구하던 기대 발음 추출
오류와 `건 / 도구의`를 단어 내부 쉼으로 보던 경계 오탐을 수정했다.

인위적 끊김 시험에서는 Qwen이 단어 끝을 무음 앞에 잘못 잡아 뒤의 발화를
미배정했다. v3는 그 미배정 구간의 지속적인 발화도 근거로 사용한다. 저장된
실제 Whisper·Qwen 결과와 동일 파형에 CPU 판정만 다시 적용한
`review-v3-replay.json`에서 정상 두 샘플 통과와 삽입한 끊김 탐지를 모두 확인했다.
마지막 CPU 판정 변경 뒤에 모델 추론을 다시 실행한 것은 아니다. 원래 실행의
`validation.json`과 `review-20260906-143444-ed6d18.json`은 당시의 실패 판정을 보존한다.
이번 결과를 전체 강의의 자연스러움 개선율이나 완벽성으로 해석하지 않는다.

`scripts/validate_prosody_live.py`는 현재 제작 프로필로 샘플 생성부터 교차 검수와
인위적 끊김 시험까지 실행한다. `--review-existing <검증폴더>`는 새 합성 없이
재검한다. 음성과 모델 원본은 변경하지 않는다.

전체 대본의 회상 질문 28곳에 있는 스텝 끝 무음도 지원했다. 같은 페이지의
다음 답변 스텝 앞에 무음을 전달하며, 전체 1,864개 발화와 28개 대기를 파싱했다.
강의 소스는 수정하지 않았다.

2026-09-05 제작 도구 보완: 화면뿐 아니라 대본·자료·순서를 `.production-input`에
함께 고정하고 TTS 전에 정합성을 검사한다. 한 챕터의 레슨별 출력도 같은 판본을
쓴다. sourceContract를 음성·타임라인·검수 결과로 전달하고, 촬영 전 실제 타임라인으로
구도 후보와 videoBeats를 다시 검사한다. 상세 계약은 ARCHITECTURE의 현행 제작 절과
udemy-agent/NARRATION-PIPELINE.md가 정본이다. 강의 덱 화면과 대본은 이 도구 작업에서 변경하지 않았다.

## 1. 프로젝트 범위

한국어 강의 대본을 개인 음성으로 생성하고 자막·타임라인·영상으로 제작한다.
제작 환경은 MacBook Pro 16 M4 Max, 통합 메모리 36GB, Python 3.13.12다.
현재 음성은 Qwen3-TTS 1.7B Base + `jaeho-ko-r16-v1` LoRA 강도 `0.60`이다.
모델 비교와 LoRA 선택은 완료됐다.

## 3. 확보한 사용자 음성

원본은 2026-08-26부터 다음 로컬 정본 저장소에 있다.

`/Users/jaehoseo/Desktop/vswrk/edu/local-tts-engine/data/private/voice/`

이관 전 위치인 `udemy-agent/deck/voice/`는 삭제하지 않고 복구용 사본으로
남겼다. 두 위치의 20개 파일은 이관 직후 차이가 없음을 확인했다.

| 구분 | 상태 |
| --- | --- |
| 원본 다섯 파일 | DJI Mic Mini 2 녹음, 합계 약 54분 23초 |
| 정제 마스터 | 48kHz, mono, PCM 24-bit, 유효 53분 20초 |
| 업로드용 파생본 | 48kHz, mono, MP3 256kbps |
| 파일별 음량 | 약 -25.11~-25.05 LUFS |
| 파일별 true peak | 약 -2.96~-2.84 dBTP |

사용자는 말이 꼬이면 약 3초 멈춘 뒤 다시 읽는 규칙을 지켰고, 표시된 실패
테이크는 정제본에서 제거했다. EQ, 노이즈 제거, 음색 보정은 적용하지 않았다.

정제 파일과 수치의 정본:

- `deck/voice/training/pvc/README.md`
- `deck/voice/training/pvc/manifest.json`
- `deck/voice/training/pvc/master-wav/`

53분 전체를 첫 zero-shot 요청에 쓰지 않는다. 잡음·호흡·억양이 안정적인
10~20초를 먼저 고르고 정확한 전사문과 짝지어 사용한다. 전체 세트는 향후
미세조정 또는 다른 공급자 학습용으로 보존한다.

## 4. 기존 제작 파이프라인

정본 문서:

- `/Users/jaehoseo/Desktop/vswrk/edu/udemy-agent/NARRATION-PIPELINE.md`
- `/Users/jaehoseo/Desktop/vswrk/edu/udemy-agent/VIDEO-PACING-GUIDELINES.md`
- `/Users/jaehoseo/Desktop/vswrk/edu/udemy-agent/deck/narration/README.md`

Node 미디어 런타임 `deck/tools/course-media.mjs`와 전용 진입점
`captions.mjs`, `capture.mjs`는 다음을 구현한다. `narration.mjs`는 과거 명령의
하위 호환용이다.

- 마크다운 대본을 화면 ID와 스텝 단위로 파싱
- 자막 원문과 발음 사전 적용 TTS 문장을 분리
- 공급자·모델·목소리·텍스트를 포함한 해시 캐시
- 실제 오디오 길이 측정, 타임라인과 SRT/VTT 생성
- Playwright 슬라이드 자동 캡처
- H.264/AAC 영상과 화면 자막 합성본 생성
- 한 스텝만 고치면 해당 음성 클립만 재생성

로컬 엔진은 이 계약을 대체하지 않고 새로운 TTS provider로 연결한다.

## 9. 2026-08-25 현재 구현 상태

모델 A/B 선택은 완료된 과거 작업이다. 현재 제작 프로필은 Qwen3-TTS 1.7B
Base BF16 + 짧은 참조 음성·전사 + `jaeho-ko-r16-v1` LoRA 강도 `0.60`이다.
Chatterbox V3는 제외했다. `0.60`은 LoRA `1.00`에서 두드러진 사람 발화 노이즈와
과한 AI 특성을 줄여 장시간 강의에 가장 적합하다고 사용자가 승인한 값이다.

강의용 생성기는 `src/local_tts_engine/course_pilot.py`다.

- 같은 슬라이드의 2~4개 스텝, 최대 300자를 한 호흡으로 생성
- 생성 온도 0.75, top_p 0.95
- 앞뒤 무음 정리, 24ms 경계 페이드
- 같은 슬라이드 클립 간격 200ms, 슬라이드 간격 750ms
- 700ms 초과 내부 무음만 480ms로 축소
- `config/production-pronunciation.ko.json`에서 제작용 발음 예외 관리
- Qwen3-ForcedAligner 0.6B 8-bit로 실제 단어 시각 생성
- `export_udemy.py`가 정렬값을 기존 Node 타임라인 계약으로 전달

`udemy-agent/deck/tools/captions.mjs`는 정렬 단어로 SRT/VTT와 화면 자막을
만든다. `capture.mjs`는 다음 스텝 첫 단어 120ms 전에 화면을 전환한다.
Playwright의 가변 녹화 지연은 마젠타 동기 마커 프레임으로 측정해 제거한다.

### 완성된 산출물 (2026-08-25)

| 항목 | 경로 |
| --- | --- |
| TTS 음성·매니페스트 | `artifacts/course-pilots/2026-08-25/qwen3-tts-first-10m/` |
| 최종 자막 영상 | `deck/render/narration/qwen3-first-10m/qwen3-first-10m-captioned.mp4` |
| SRT/VTT/타임라인 | 같은 `qwen3-first-10m/` 디렉터리 |

영상 수치:

- 9분 47.4초, 19개 화면, 67개 스텝, 26개 음성 청크
- H.264 1920×1080 25fps, AAC 48kHz mono, 55.2 MB, 자막 199큐
- 첫 슬라이드: `open-cover` (1.3초 시작), 마지막: `promptify-promptify`
- Metal 메모리 peak 11.6 GB

이전 old 산출물:

- `artifacts/course-pilots/2026-08-23-old/`, `2026-08-24-old/`
- `deck/render/narration/qwen3-first-10m-old/`, `qwen3-first-5m-old/`

### 레슨 단위 preset 설계 (2026-08-25)

영상 수정 시 해당 레슨 구간만 재합성할 수 있도록 레슨 단위 preset을
`udemy-agent/deck/narration.config.json`에 추가했다.

| preset | 제목 | 슬라이드 범위 | 장수 |
| --- | --- | --- | --- |
| `ch00` | CH00 강의 프레임 전체 | `open-cover` → `open-say-2` | 11장 |
| `ch01-l00` | CH01 L00 챕터 프레임 | `frame-genai-cover` | 1장 |
| `ch01-l01` | CH01 L01 일을 시키는 방법 | `promptify-shot` → `promptify-lanes-gap` | 18장 |
| `ch01-l02` | CH01 L02 LLM은 무엇인가 | `llm-open` → `llm-recap` | 28장 |
| `ch01-l03` | CH01 L03 생성형 AI의 구조적 한계 | `genai-limit-say-1` → `genai-limit-say-3` | 7장 |
| `ch01-l04` | CH01 L04 RAG란 무엇인가 | `rag-open` → `rag-recap` | 8장 |
| `ch01-l05` | CH01 L05 RAG 도입 이유 | `rag-why-say` → `rag-why-def` | 3장 |
| `ch01-l06` | CH01 L06 RAG가 못 하는 것 | `rag-why-split` → `rag-gap-say-3` | 6장 |
| `ch01-l07` | CH01 L07 Agent란 무엇인가 | `agent-def-warn-1` → `agent-def-scale` | 13장 |
| `ch01-l08` | CH01 L08 RAG와 Agent의 차이 | `rag-vs-agent-say` → `rag-vs-agent-design-principle` | 6장 |
| `ch01-l09` | CH01 L09 Workflow와 Agent의 차이 | `rag-vs-agent-case` → `workflow-check-2` | 13장 |
| `ch01-l10` | CH01 L10 Agent가 필요한 이유 | `agent-cost-say` → `agent-cost-check` | 3장 |
| `ch01-l11` | CH01 L11 Agent 도입 금지 | `agent-dont-warn` → `agent-dont-plan` | 4장 |
| `ch01-l00-end` | CH01 L00 챕터 요약·전환 | `frame-genai-chapter-summary` → `frame-genai-next-question` | 2장 |

### 다음 세션의 작업 순서

CH01 레슨별 TTS 생성 → export → captions → capture 순서로 진행한다.
**한 레슨씩 승인받아 다음으로 넘어간다.**

각 레슨 생성 명령 패턴:

```bash
# TTS 생성 (예: L01)
cd /Users/jaehoseo/Desktop/vswrk/edu/local-tts-engine
.venv/bin/python -m local_tts_engine.course_pilot \
  --reference artifacts/benchmarks/2026-08-23/reference.wav \
  --reference-text artifacts/benchmarks/2026-08-23/reference.txt \
  --output-dir artifacts/course-pilots/$(date +%Y-%m-%d)/ch01-l01 \
  --start-chapter ch01 \
  --start-slide promptify-shot \
  --target-seconds <예상초> \
  --seed 20260825

# export
.venv/bin/python -m local_tts_engine.export_udemy \
  --source-dir artifacts/course-pilots/$(date +%Y-%m-%d)/ch01-l01 \
  --deck-root /Users/jaehoseo/Desktop/vswrk/edu/udemy-agent/deck \
  --preset ch01-l01 \
  --provider qwen3-local

# captions + capture
cd /Users/jaehoseo/Desktop/vswrk/edu/udemy-agent/deck
node tools/captions.mjs --preset ch01-l01 --provider qwen3-local
node tools/capture.mjs --preset ch01-l01 --provider qwen3-local --burn-captions
```

레슨별 예상 길이 (대본 글자 수 기준 추정, 실측 필요):

| preset | 예상 길이 | `--target-seconds` |
| --- | --- | --- |
| ch01-l01 | ~15분 | 900 |
| ch01-l02 | ~25분 | 1500 |
| ch01-l03 | ~5분 | 300 |
| ch01-l04 | ~7분 | 420 |
| ch01-l05 | ~3분 | 180 |
| ch01-l06 | ~6분 | 360 |
| ch01-l07 | ~11분 | 660 |
| ch01-l08 | ~6분 | 360 |
| ch01-l09 | ~10분 | 600 |
| ch01-l10 | ~2분 | 120 |
| ch01-l11 | ~4분 | 240 |

승인 전에는 다음 레슨으로 넘어가지 않는다.

## 10. 로컬 제작 앱 고도화 (2026-08-26)

Electron 앱에 독립된 `목소리 만들기` 화면을 추가했다.

- 텍스트만 입력해 2~8개 발화 후보 생성
- 전역 설정의 병렬 수에 따라 최대 2개씩 동시 생성
- 후보별 오디오 재생, 하나를 선택해 `selected.wav`로 보관
- 후보와 선택 결과는 `output/voices/<date>/<name>/`에 저장
- 각 후보는 새 시드로 추론하며 기존 오디오 결과를 읽지 않음
- 텍스트 후보의 서로 다른 시드는 비교 선택을 위한 의도적 다양성이다. 강의 TTS는
  고정된 제작 시드와 의미 단위 청킹을 사용하므로 후보 간 편차를 강의 생성의
  불안정성으로 해석하지 않는다.

모델 설정의 이전 버전에는 다음 경로 선택 항목이 있었고 값은 Git에서 제외되는
`artifacts/app-settings.json`에 저장된다.

- 강의 소스 프로젝트
- 내 목소리 원본
- TTS 작업 결과
- 텍스트 목소리 후보
- 자막·타임라인 결과
- 완성 영상 결과
- 참조 음성과 참조 전사문

이 개별 출력 경로 UI는 2026-08-27 단일 `output` 폴더 설정으로 대체됐다.
`udemy-agent`에는 강의 대본·슬라이드 UI와 캡처 실행 코드만 남는다.

앱의 강의 영상 제작은 항상 `course_pilot --no-cache`와 캡처 `--no-cache`를
사용한다. 음성 클립, ForcedAligner JSON, Chromium HTTP 캐시를 재사용하지
않는다. 독립 CLI의 기본 캐시 동작은 이전 호환성을 위해 유지한다.

## 11. 제작 화면 정보 패널 개편 (2026-08-26)

이 절의 정보 중심 패널은 2026-08-27 제품화 정리에서 더 간결한 작업 요약으로
대체됐다.

- 새 영상 화면의 `02 진행 상황` 빈 대기 화면을 `제작 브리프`로 교체했다.
  선택 범위, 제작 음성, 산출물, 자막 방식, 캐시 정책, 검증 단계와 저장 위치를
  실행 전에 실시간으로 보여 준다. 실행 중에는 단계 진행, 완료 후에는 검수 결과로
  같은 공간의 역할이 전환된다.
- 목소리 만들기의 안내 카드를 `발화 설계` 패널로 교체했다. 글자 수, 줄바꿈 호흡,
  후보 수, 병렬 계획, 현재 LoRA 프로필과 감지한 영문 용어를 실시간으로 표시한다.
- 텍스트 후보 입력의 줄바꿈을 더 이상 공백으로 합치지 않는다. 비어 있지 않은 각
  줄을 호흡 단위로 보존해 TTS에 전달한다.
- 큰 장식 아이콘과 중앙 정렬 안내문을 제거하고, 낮은 대비의 정보 카드와 짧은
  상태 라벨을 사용했다. `prefers-reduced-motion`도 반영한다.

## 12. 제작 탐색과 사람 검수 UX (2026-08-27)

- 새 영상 화면에 챕터 빠른 이동, 이전·다음 페이지, 챕터 전체 선택을 추가했다.
  현재 선택 범위의 챕터·페이지·스텝 수를 실행 전에 계산한다.
- 최근 결과에 이름 검색과 강의·목소리·편집·확인 필요 필터, 전체·청취 승인·확인
  필요 요약을 추가했다.
- 자동 검증과 사람의 최종 청취 판단을 분리했다. `청취 승인`은 각 결과의
  `validation-report.json` 안 `review` 필드에 기록하며 승인 취소도 지원한다.
- 모델 설정이 승인된 제작 프로필 `Qwen3-TTS + jaeho-ko-r16-v1 + 0.60`과 다르면
  실험 설정 경고를 표시하고, 승인값을 복원하는 버튼을 제공한다.
- 비차단 완료·오류 알림을 추가했다. 이때 넣었던 화면 전환·실행 단축키는 이후
  시각적 복잡도를 줄이는 과정에서 제거했다.

## 13. 결과물 폴더 단순화 (2026-08-27)

- 설정 화면에서 TTS·목소리 후보·자막·영상의 개별 출력 경로를 제거했다.
- 사용자는 `강의 소스`, `내 목소리 원본`, `결과물 폴더`만 선택한다. 참조 음성과
  전사문은 `고급 입력 설정`에 접어 두었다.
- 기본 결과물 폴더는 프로젝트의 `output/`이다. 앱이 `videos`, `voices`, `tts`,
  `projects`, `edits` 하위 폴더를 자동 생성한다.
- 기존 `artifacts/` 결과는 이동하거나 삭제하지 않는다. 최근 결과 목록과 열기,
  Finder 표시, 청취 승인은 레거시 경로에서도 계속 동작한다.

## 14. 기본 작업 흐름 제품화 (2026-08-27)

- 새 영상의 기본 범위를 페이지 번호가 아닌 `narration.config.json`의 실제 레슨
  이름으로 바꿨다. 개발용·파일럿 preset은 레슨 목록에서 제외한다.
- 결과 이름, 산출물 종류와 자막 옵션은 `세부 설정`으로 내렸다. 기본 화면은 레슨
  선택과 제작 시작에 집중한다.
- 제작 브리프의 캐시·동기화·검증 카드와 저장 경로를 제거하고 범위·목소리·결과만
  남겼다. 처리 방식은 접힌 설명에서 확인한다.
- 목소리 만들기의 별도 발화 분석 패널을 제거했다. 텍스트, 짧은 입력 피드백,
  생성 버튼만 기본 노출하고 후보 수와 결과 이름은 접었다.
- 최근 결과의 통계 카드와 seed 같은 개발 메타데이터를 제거하고, 사용자 언어로
  `파일 확인 완료`와 `직접 확인 완료`만 표시한다.
- 모델·어댑터·LoRA 강도·병렬 수와 파인튜닝은 `고급 모델 설정` 안으로 이동했다.

## 15. 가독성 정리 (2026-08-27)

- 사이드바의 `⌘1`~`⌘4` 표시와 숨은 키보드 단축키 동작을 제거했다.
- 실제 화면에 노출되는 글자는 최소 10px로 올렸다. 주요 입력·버튼·레슨·결과
  텍스트는 11~14px, 화면 제목은 36px로 조정했다.
- 이전 UI에서 제거된 제작 브리프, 결과 통계, 발화 분석 패널의 미사용 스타일도
  함께 삭제했다.

## 16. 레슨 결과명·백그라운드 캡처·설정 UI 정리 (2026-08-27)

- 레슨 탭에서 만든 최종 MP4는 내부 작업 slug 대신 `narration.config.json`의
  레슨 제목을 파일명으로 사용한다.
- 같은 제목의 기존 영상은 삭제하거나 덮어쓰지 않는다. `레슨 제목.mp4`,
  `레슨 제목 (2).mp4`, `레슨 제목 (3).mp4` 순서로 보존한다.
- 최근 결과 화면도 내부 작업명 대신 레슨 제목을 표시하고, 레슨 제목 검색을
  지원한다. 열기·Finder·검수 기록은 기존 내부 작업 식별자를 계속 사용한다.
- Playwright 녹화는 기존처럼 headless와 Metal GPU 옵션을 유지한다. 캡처가
  시작한 Vite 서버만 `capture` 모드로 실행해 `server.open`을 끄므로 기본
  브라우저가 전면에 나타나지 않는다. 일반 `npm run dev`는 기존처럼 브라우저를
  연다.
- 사이드바 하단 로컬 환경 카드를 목소리·상태·설정 진입이 구분되는 작은 엔진
  컨트롤로 교체했다.
- `Voice Studio 설정` 팝업을 920px 너비의 2열 구조로 넓히고, 파일·결과물과
  제작 목소리를 별도 카드로 나눴다. 승인된 제작값과 고급 설정의 위계는 그대로
  유지한다.

## 17. 캡처 격리·재시도·작업 중지 보강 (2026-08-27)

- 영상 작업을 시작하면 TTS보다 먼저 그 시점의 강의 덱을 정적 사이트로 빌드해
  작업 폴더의 `.capture-site`에 고정한다. 이후 CH02 등 다른 소스를 수정·저장해도
  진행 중인 캡처는 이 복사본만 읽는다. 작업이 끝나거나 실패하면 복사본을 지운다.
- 영상 캡처는 열려 있던 5180 개발 서버를 재사용하지 않는다. 고정한 정적 사이트를
  빈 로컬 포트에서만 제공하므로 다른 브라우저 탭의 덱 동기 상태와 섞이지 않고
  Vite HMR도 존재하지 않는다. 실제 문서 새로고침이 생기면 `actual=undefined` 대신
  원인을 설명하는 오류를 남긴다.
- 캡처 실패 시 브라우저·녹화 컨텍스트·전용 서버를 항상 정리한다. 앱은 생성된
  음성과 타임라인을 버리지 않고 캡처만 한 번 자동 재시도한다.
- 작업 중지는 버튼을 누르는 즉시 `중지 요청 중`으로 바뀐다. 현재 하위 프로세스
  그룹에 `SIGTERM`을 보내고 3초 뒤에도 남아 있으면 `SIGKILL`로 종료한다. 단계 사이에
  중지를 눌러도 다음 단계가 새로 시작되지 않는다.

## 18. 제품화 준비 1차 정리 (2026-09-03)

- 첫 판매 형태는 서버형 SaaS가 아니라 Apple Silicon용 로컬 데스크톱 유료
  베타로 정했다. 개인 음성·대본·결과물을 로컬에 두는 현재 장점을 유지한다.
- 사용자 홈에 박혀 있던 강의 소스 경로와 Homebrew 실행 파일 경로를 이식 가능한
  탐색 방식으로 바꿨다. 환경 변수로 명시적 재정의도 가능하다.
- 텍스트 목소리, 기존 영상 편집, 강의 영상 자동 제작의 준비 조건을 분리했다.
  텍스트 목소리는 `udemy-agent` 강의 소스 없이도 참조 음성·전사문과 로컬 모델
  환경만 있으면 실행할 수 있다.
- `npm run doctor`는 기능별 준비 상태를 보여 주고 기본 출력에서는 개인 경로를
  숨긴다. `npm test`는 Electron과 Python 테스트를 함께 실행한다.
- 현재 개인 제작 프로필과 LoRA 강도 0.60은 변경하지 않았다.
- 제품 범위, 배포 차단 항목, 라이선스·YouTube 정책과 베타 순서는
  `docs/PRODUCTIZATION.md`가 정본이다.

## 19. 제품 UI 정보 위계 정리 (2026-09-03)

- 상단의 상시 `준비됨` 문구를 작은 상태 점으로 바꿨다. 상세 뜻은 hover 또는
  키보드 focus에서만 툴팁으로 보이고, 실행·완료·오류에 따라 색과 설명이 바뀐다.
- 새 영상 화면의 오른쪽 요약은 입력 화면과 같은 정보를 반복해 삭제했다. 대기
  중에는 오른쪽 패널 전체를 숨기고, 작업을 시작한 뒤 진행 단계와 완료 검수에만
  사용한다.
- 최근 결과는 `열기`만 기본 행동으로 남겼다. `확인 완료`와 `Finder에서 보기`는
  더 보기 메뉴로 옮겼고, 자동 검증·직접 확인 상태는 툴팁이 있는 아이콘으로 줄였다.
- 더 보기 메뉴는 화면 아래 공간이 부족하면 위쪽으로 열리며 열린 결과 행을 다른
  행보다 위에 표시한다. 결과 카드가 hover에서 위로 뜨는 모션과 더 보기 버튼의
  불필요한 hover 말풍선은 제거했다.
- 결과 카드의 위치 이동만 제거하고 hover·키보드 focus의 테두리·배경 피드백은
  유지한다. 더 보기 메뉴는 메뉴 밖을 클릭하거나 `Esc`를 누르면 닫힌다.
- 사이드바 하단 목소리 카드는 장식과 상태 문장을 제거하고 현재 프로필명, 강도와
  환경 상태 점만 표시한다.
- 설정 팝업은 현재 목소리와 결과물 폴더만 기본 노출한다. 강의·음성 원본 경로와
  모델·어댑터 설정은 각각 접힌 섹션으로 분리했다.
- 위 구조를 고정하는 UI 계약 테스트 3개를 추가했다.
- 강의 대본에서 한 줄 전체가 `(…)` 또는 `（…）`인 문장은 제작 지시문으로 보고
  TTS·자막 입력에서 제외한다. 강조 마커로 감싼 괄호 지시문도 동일하다. 문장
  중간의 괄호 표현은 실제 본문이므로 유지한다.
- 같은 화면 스텝 안에서 자동 애니메이션을 기다리는 단독 줄 `[Ns]`는 강제
  무음으로 처리한다. 스텝을 내부 음성 조각으로 나눠 지정한 무음을 넣고, 단어
  정렬 뒤에는 다시 하나의 화면 스텝으로 합친다. 마커는 TTS·자막에 노출하지
  않으며 0.1~10초만 허용한다. CH02 138페이지 `toolpick-camera-in` 2스텝에
  `[2s]`를 적용했다.

## 20. 자동 발음 검수·페이지 단위 음성 교체 (2026-09-03)

- 자막 원문은 유지하면서 TTS 입력만 구조적으로 정규화한다. 현재 사용자 승인
  발음은 `Runtime → 런타임`, `Context → 컨텍스트`, `RAG → 래그`,
  `IBM → 아이비엠`, `Meta → 메타`, `Qwen3.6-27B → 큐웬삼점육 이십칠비`,
  `50개 → 오십 개`다. 모델명은 큐웬과 버전, 크기와 단위를 붙이고 버전과 크기
  사이만 한 칸 띄운다.
- 덱 사전과 로컬 제작 사전을 단순 순차 치환하지 않고 같은 `from` 키는 로컬
  값이 실제로 덮어쓰게 합친다. 긴 용어를 먼저 적용해 `Multi-Agent` 안의
  `Agent`가 먼저 소모되는 문제도 막았다.
- 제작 모델과 같은 Qwen 계열을 평가기로 쓰지 않는다. 약 1.61GB의
  `mlx-community/whisper-large-v3-turbo-asr-fp16`을 독립 판독기로 사용한다.

## 21. 검수를 발음 공간으로 옮기고 생성을 조기 종료로 바꿈 (2026-09-03)

### 왜 바꿨나

148페이지가 `지정 발음 불일치`로 실패했지만 음성은 정확했다. Whisper는 소리를
제대로 들었고 자기 표기법으로 적었을 뿐이다.

```
대본 발음: … 왼쪽은 로컬에서 돌리는 이십칠 비, 큐웬삼점육 이십칠비입니다 …
받아쓰기: … 왼쪽은 로컬에서 돌리는 27B, QN 3.6 27B입니다 …
```

받아쓰기를 철자로 비교하는 한 숫자와 모델명이 들어간 페이지는 거의 항상
실패한다. 검수기가 아니라 비교 계층이 틀렸다.

### 발음 공간 비교

`src/local_tts_engine/korean_phonetics.py`가 모든 비교의 기준이 된다.

- 숫자·모델명 정규화와 발음 사전을 양쪽에 적용한 뒤 한글을 자모로 분해한다.
- 전사에 남은 로마자는 한국어 자모음 이름으로 읽는다. `QN` → `큐엔`.
- 현대 한국어가 구분하지 않는 차이만 접는다. 된소리(`ㄲ→ㄱ`)와 `ㅐ/ㅔ` 합류는
  접고, 거센소리는 접지 않는다. `런타임 → 런팀` 같은 오독이 살아 있어야 한다.
- 용어 검사는 부분 문자열이 아니라 Sellers 근사 부분 일치 거리로 판단하고,
  기대 횟수보다 적게 들리면 누락으로 본다.

실측 분리도 (`MAX_PHONETIC_ERROR_RATE = 0.10`):

| 상황 | 철자 거리 | 발음 거리 |
| --- | --- | --- |
| ASR 표기차 (`QN 3.6 27B`, 짧은 청크) | 0.154 | 0.029 |
| ASR 표기차 (실제 148페이지) | 0.048 | 0.009 |
| 긴 문장 안 한 단어 오독 | 0.053 | 0.021 |
| 문장 하나 누락 | 0.487 | 0.480 |
| 절반 날아감 | 0.633 | 0.610 |

철자 거리로는 표기차와 실제 오류가 겹치지만 발음 거리로는 열 배 이상 벌어진다.
그래서 페이지를 실패시키는 관문은 발음 거리를 읽는다.

### 3단 판정

- **통과** — 재시도 없이 다음 청크로 간다.
- **확인 권장(경고)** — 용어가 애매하게 들렸다. 다른 시드로 다시 시도하되,
  끝까지 애매하면 제작은 완료하고 구간만 표시한다.
- **재생성 필요(실패)** — 받아쓰기 불일치, 음성 신호 부족, 과도한 무음,
  클리핑, 발화 속도 이상, 명백한 발음 불일치.

경고가 **모든 시드에서 똑같이 재현되면** ASR 노이즈가 아니라 모델이 일관되게
다르게 읽는 것이므로 실패로 승격한다. 이 경우 같은 조건의 재생성으로는 잘
고쳐지지 않으므로 발음 사전에 항목을 추가하는 편이 빠르다.

1차 판독에서 걸린 청크는 다른 디코딩 온도로 한 번 더 읽고 둘 중 관대한 결과를
취한다. 모든 판독을 견딘 근거만 페이지를 표시할 수 있다.

### 조기 종료 생성

이전에는 위험 청크마다 후보 3개를 **무조건** 만든 뒤 별도 Whisper 단계에서
골랐다. 이제 판독기를 생성기보다 먼저 올려 함께 상주시키고, 청크마다

    생성 → 즉시 받아쓰기 → 깨끗하면 종료, 아니면 다음 시드

로 진행한다. 결과:

- 정상 청크는 생성 1회로 끝난다. 레슨 전체 제작이 크게 빨라진다.
- 대신 **모든** 청크를 검수한다. 발음 사전 용어가 없는 청크의 누락·더듬음도
  잡힌다.
- 정말 안 되는 청크에만 시도를 넉넉히 쓴다 (`--quality-attempts`, 기본 4).
- Metal 메모리는 TTS 약 11.6GB + Whisper 약 1.6GB로 함께 상주한다. 정렬 모델은
  둘을 해제한 뒤 별도 단계에서 올린다. manifest의
  `performance.peakProcessMetalMemoryGb`에 실제 최대치를 기록한다.

청크당 재시도 판단은 `course_pilot.resolve_chunk_take`로 분리해 GPU 없이
단위 테스트한다.

### UX

- 음성 의심 구간은 제작 실패가 아니다. 파일·영상 검증만 제작 성패를 가르고,
  음성 판정은 별도 확인 항목으로 완료 패널과 최근 결과에 실린다.
- 완료 패널과 최근 결과 모두 `2:14–2:21 · 148페이지 · 지정 발음 불일치 · 래그`
  형태로 시간대·페이지·사유·걸린 용어를 보여 준다.
- 구간을 클릭하면 영상 편집의 `목소리 교체 → 페이지에서 새로 생성`이 그 영상과
  그 페이지로 채워진 채 열린다. 음성만 만든 결과는 열 영상이 없으므로 표시만
  하고 클릭을 제공하지 않는다.
- 목소리 후보 카드에 자동 검수 결과를 함께 적어, 귀로 고르기 전에 기계가 무엇을
  들었는지 볼 수 있다.

### 같이 고친 버그

- `trim_and_fade_audio`의 조기 반환 경로가 `shortenedSilenceCount`와
  `shortenedSilenceMs`를 빼고 반환해, 무음이거나 한 프레임보다 짧은 클립에서
  `KeyError`로 런이 죽었다. 게다가 그 불완전한 딕셔너리가 클립 사이드카로
  저장돼 이후 캐시 히트까지 오염시켰다. 네 키를 항상 채우고, 예전 사이드카는
  읽는 시점에 보정한다.
- 완성 영상은 `output/videos/<작업>/`로 옮겨지는데 `timeline.json`은
  `output/projects/<작업>/`에 남았다. 그래서 발행된 영상을 편집에 다시
  올리면 페이지 경계를 잃고, **페이지 범위 목소리 교체가 조용히 전체 음성
  교체로 바뀌었다.** 이제 발행 시 자기 이름의 타임라인(`<영상>.timeline.json`)을
  함께 두고, 예전 결과는 프로젝트 폴더에서 찾아 보완한다.
- 같은 작업 이름을 다시 렌더하면 한 폴더에 여러 mp4가 쌓이는데 `findVideo`가
  사전순 마지막을 골라 **가장 오래된** 파일을 열었다. 수정 시각 기준으로
  바꾸고, 타임라인도 영상별로 짝지어 회차가 섞이지 않게 했다.
- 페이지 타임라인이 없는 영상에 페이지 범위 교체를 시도하면 이유를 밝히고
  멈춘다. 전체 음성을 덮는 것보다 거절이 낫다.
- 후보 생성은 이제 한 번에 하나씩 돌린다. 후보마다 생성기와 판독기가 함께
  올라가므로 병렬 2개는 36GB에서 위험했다.
- 자모 변환이 `str.isdigit()`을 믿어 `①`·`²` 같은 문자를 `int()`에 넘겨
  런 중간에 예외로 죽을 수 있었다. ASCII 숫자만 읽는다.

### 테스트

Python 98개, 앱 49개. 새로 덮은 영역:

- `tests/test_korean_phonetics.py` — 자모 분해, 로마자 음역, 접는 차이와
  접지 않는 차이, 근사 일치 거리, 반복 용어 세기.
- `tests/test_speech_quality.py` — 148페이지 실제 데이터 회귀, 발음 관문과
  철자 관문의 분리, 경고와 실패의 구분, 시드 반복 시 승격.
- `tests/test_chunk_takes.py` — 조기 종료, 재시도 예산, 판독기를 끈 경로.
- `electron-app/pipeline-utils.test.mjs` — 확인 구간 매핑, 타임라인 후보 경로,
  회차 충돌, 페이지 교체 거절 사유.
- `electron-app/renderer-ui.test.mjs` — 완료 패널이 실제로 그려지는지, 클릭이
  편집을 채우는지, 음성 판정이 제작 실패로 새지 않는지.

### 남은 숫자를 읽게 만듦 (실제 강의 전체 검사 후)

기존 코드는 단위가 붙은 숫자(`50개`)와 모델명, 소수만 정규화하고 나머지는 숫자
그대로 TTS에 넘겼다. 넘겨진 숫자는 모델이 마음대로 읽는다 — 사용자가 처음
지적한 `같은 50개라도 → 같은 호십개라도`가 바로 그 결과다.

실제로 만든 31개 매니페스트, 1094개 스텝, 75,766자를 훑어 확인한 결과 그렇게
넘어가던 표현이 **133종**이었다.

```
50이라는 · 160토큰 · 8천 토큰 · 4천억 개 · 15조 토큰 · 40 대 1 · 1, 2, 3, 4.
99% · 10년 · 2026년 8월 · 200페이지 · 10분의 1 · 1등 · 90에서 95로 …
```

`read_remaining_numbers`가 사전까지 적용한 뒤에도 숫자로 남은 것을 한자어 수로
읽는다. 사전이 먼저 돌기 때문에 숫자가 든 사전 항목(`GPT-4o`)은 그대로
우선하고, 글자에 둘러싸인 숫자(`n8n`, `4o`)는 식별자로 보고 두 번 다시 건드리지
않는다. 붙여 읽으므로 조사가 와도(`오십이라는`) 단위가 와도(`백육십토큰`) 자연스럽다.

검사 후 강의 전체에 숫자로 남은 문자는 `n8n`과 `4o` 둘뿐이고, 지정 발음 314건을
원문 표기 그대로의 받아쓰기와 대조했을 때 불일치는 0건이다.

**청취하며 확인해 볼 곳.** 아래처럼 뜻에 따라 고유어와 한자어가 갈리는 단위는
2026-09-04 자연스러움 검사에서도 자동 판단하지 않는다. 어색하면
`config/production-pronunciation.ko.json`에 구체적인 문구를 넣는다(사전이
자연스러움 검사보다 먼저 돈다).

| 페이지 | 지금 읽는 방식 | 고유어라면 |
| --- | --- | --- |
| 54p | `1턴, 2턴` → 일턴, 이턴 | 한 턴, 두 턴 |
| 36p | `1배에는`, `2배였습니다` → 일배, 이배 | 한 배, 두 배 |
| 56p | `1시간 옵션` → 일 시간 | 한 시간 |
| 36p | `GPT-4랑` → …사랑 | 사와 / 사 랑 |

### 아직 확인하지 않은 것

이 세션은 MLX를 실행할 수 없어 실제 음성 생성 스모크는 돌리지 못했다.
`docs/PRODUCTIZATION.md`가 아니라 여기 적어 둔다. 다음 실행에서 확인할 것:

```bash
cd /Users/jaehoseo/Desktop/vswrk/edu/local-tts-engine
npm run check          # doctor --strict + Python·앱 테스트

# 148페이지 한 장으로 회귀 확인 (이전에 오탐으로 실패하던 페이지)
.venv/bin/python -m local_tts_engine.course_pilot \
  --reference artifacts/benchmarks/2026-08-23/reference.wav \
  --reference-text artifacts/benchmarks/2026-08-23/reference.txt \
  --output-dir output/tts/$(date +%Y-%m-%d)/regression-p148 \
  --start-page 148 --end-page 148 --target-seconds 30 \
  --adapter artifacts/finetune-runs/2026-08-25/jaeho-ko-r16-v1/adapters \
  --adapter-scale 0.60 --no-cache
```

확인할 값:

- 로그에 `[자동 음성 검수 n/n] … 후보 1/1 통과`가 보이는가 (조기 종료 동작).
- `manifest.json`의 `quality.summary.ok`가 참인가.
- `performance.peakProcessMetalMemoryGb`가 36GB 대비 여유가 있는가.
  이 값이 위험하면 `--quality-attempts 1`이나 `--no-auto-quality`가 탈출구다.

## 22. 한국어 자연스러움 사전검사 (2026-09-04)

148페이지에서 실제 음성은 `방금 본 십 개, 백 개, 천 개`였고 Whisper는 이를
`10개, 100개, 1000개`로 적어 통과시켰다. 당시 기대 발음문부터 `십 개`였으므로
Whisper 오검출이 아니라 생성 전 언어 판단의 누락이었다.

`src/local_tts_engine/korean_naturalness.py`를 발음·숫자 정규화 앞에 추가했다.

- `개·명·가지`의 1~99는 고유어 관형형으로 만든다. `10개 → 열 개`,
  `20개 → 스무 개`, `34개 → 서른네 개`다.
- 100 이상은 기존 한자어 규칙이 처리한다. 따라서 `100개 → 백 개`,
  `1000개 → 천 개`다.
- 사용자가 이미 승인한 `50개 → 오십 개`는 제작 예외로 보존한다.
- `6개월`의 `개`를 물건 수량으로 오인하지 않으며, `번·장·시간`처럼 문맥이
  필요한 단위는 자동 변환하지 않는다.
- 발음 사전은 자연스러움 검사보다 먼저 적용하므로 더 구체적인 강의 문구가 일반
  규칙을 덮어쓸 수 있다.
- 검사 결정은 각 엔트리와 manifest의 `naturalness`에 남기고 앱의
  `validation-report.json`에도 전달한다.

실제 CH02 전체 대본을 정적으로 검사했을 때 148페이지의 3번 스텝은
`열 개, 백 개, 천 개`가 됐다. 강의 전체에서 이 계층이 관여한 엔트리는 16개다.
새 음성 생성은 하지 않았으므로 다음 148페이지 재생성에서 최종 청취 확인한다.

## 23. 영어 발음 지정과 판독 창 교정 (2026-09-04)

### 오탐의 정체 — Whisper는 30초 창으로 읽는다

CH03 L02의 `0:01~0:32`(304페이지)는 실패로 표시됐지만 음성은 정확했다.
31초 클립을 통으로 읽히면 Whisper가 끝부분을 빠뜨린다.

```
통으로   : … 그때 분리를 봅니다  실패가 기준입니다.
뒤 8초만 : … 그때 분리를 봅니다 에이전트 수가 아니라 평가로 확인된 실패가 기준입니다
```

네 시드 전부 같은 자리에서 탈락했다. §21의 "모든 시드에서 재현되면 실패로
승격"이 **ASR 아티팩트를 모델 오독으로 승격**시킨 것이다.

`speech_quality.asr_reading_windows`가 클립을 내부 무음에서 잘라 한 판독이
창을 넘지 않게 한다. 생성 음성은 그대로 두고 **판독만** 나눈다. `MAX_CHUNK_CHARS`를
줄이면 호흡·캐시·클립 해시가 전부 바뀌므로 건드리지 않았다.

| 상황 | 통 판독 | 창 판독 |
| --- | --- | --- |
| 304p 오탐 (네 시드 전부) | 0.090–0.119 | **0.005** |
| 307p 미탐 | 0.069 | 0.009 |
| 306p 실제 오독 | 0.127–0.138 | 0.127–0.141 |
| 310p 실제 오독 | 0.107–0.224 | 0.098–0.117 |

CH01~CH03 청크 468개 중 **157개(34%)**가 28초를 넘길 것으로 예상됐다. 1회성이
아니라 3분의 1이 노출돼 있었다.

### 영문이 그대로 합성되고 있었다

덱·로컬 사전을 적용하고도 CH01~CH03에 영문 토큰 **123종 436건**이 남아
모델의 즉흥 판단에 맡겨져 있었다. 실제 판독에 잡힌 결과:

| 대본 | 들린 소리 |
| --- | --- |
| `Guardrail` | 과드레일 / 가오드레일 / 가어드레일 / 가워드 레일스 |
| `Agents-as-Tools` | 에이젠스 애시툴즈 / 앳스툴스 / H에지툴스 / 에이즈 툴즈 |
| `Instructions` | 인스트록셔스 |

또 사전이 하이픈 복합어의 **안쪽만** 바꿔 `Sub-에이전트`·`Multi-에이전트`가
**38건** 만들어지고 있었다. `Agents-as-Tools`는 `Agent` 뒤에 `s`가 붙어
아예 매칭되지 않았다.

`config/production-pronunciation.ko.json`에 복합어를 통째로 넣고(긴 항목이 먼저
적용되므로 안쪽 치환을 막는다) 영문 용어를 한글 음차로 지정했다. 25항목 → 198항목.
**CH01~CH03의 미지정 영문은 0건, 영한 하이픈 혼합도 0건이다.**

### 검수 계층이 로마자에 대해 거꾸로 돌고 있었다

기대문에 로마자가 남아 있으면 `spell_latin_letters`가 글자 이름으로 읽는다.
312페이지 실측:

| 받아쓰기 | 발음 거리 |
| --- | --- |
| `Agent AS TOOLS` (ASR이 로마자로 적음) | **0.017** 통과 |
| `에이젠스 H에지툴스` (실제로 들린 소리) | 0.376 |
| `에이전츠 애즈 툴즈` (정확히 읽었을 때) | **0.426** |

제대로 읽은 쪽이 헛소리보다 나쁜 점수를 받았고, ASR이 포기하고 로마자로 적으면
무조건 통과했다. 306페이지에서 실패시킨 용어가 312페이지에서는 통과한 이유다.

**`korean_phonetics`는 고치지 않았다.** 근본 원인은 글자 이름 규칙이 아니라
기대문에 남은 로마자이고, 사전이 그걸 없앤다. 로마자가 사라진 뒤 실측하니
관문이 바로 섰다(아래 표). 잘 도는 비교 계층을 추측으로 손대지 않는다.

### 영문 낭독은 그대로 둔다 — 측정으로 확인했다

35·179·186·190페이지는 영문을 일부러 소리내 읽는다. 이미 만든 186페이지 음성을
다시 판독해 보니 모델이 **영어를 제대로 읽는다**(발음 거리 0.019/0.005).
그래서 한글 음차로 바꾸지 않는다.

다만 용어 사전이 그 문장 **안쪽**까지 바꿔 `"Do my 봇츠 share one computer?"`가
됐다. 사전 항목에 `"literal": true`를 주면 그 구간을 사설 영역 문자로 치환해
이후 모든 규칙에서 보호하고 마지막에 되돌린다. 보호된 구간은 "읽는 법을 이미
정한 것"이므로 `unresolvedAscii`에도 잡히지 않는다.

### 사전 미지정 경고

`report_unresolved_terms`가 생성 전에 남은 영문·숫자를 페이지와 함께 출력한다.
용어를 읽는 법은 **결정**이므로 사전에 적어야 하고, 7시간을 다 들어서 발견할 일이
아니다. 다만 **막지는 않는다**: 못 읽는 용어는 나쁜 발음이지만, 식별자
(`A-2041` → 에이 이천사십일)는 다른 사실이라 그쪽만 예외를 던진다.

CH04~CH07에는 아직 **87건 40종**이 남아 있다(이번 범위 밖).

### 조사 일치

영단어를 한글로 바꾸면 끝소리가 달라져 조사가 어긋난다. `Instructions가`를
`인스트럭션가`로 만들었다. `인스트럭션스`로 바꿔 해결했고,
`Human-in-the-Loop은`처럼 구제가 안 되는 것은 더 구체적인 항목
(`휴먼 인 더 루프는`)으로 덮었다.

### 실측 결과 (CH03 L02 기존 음성 재판독)

| 페이지 | 이전 | 이후 | 걸린 용어 |
| --- | --- | --- | --- |
| 304p | 재생성 필요 | **통과** | — (오탐 제거) |
| 305p | 통과 | 확인 권장 | 가드레일즈 ← 가워드 레일스 |
| 306p | 재생성 필요 | 재생성 필요 | 에이전츠 애즈 툴즈 |
| 307p | 통과 | **재생성 필요** | 가드레일 ← 과드레일 |
| 310p | 재생성 필요 | 재생성 필요 | 가드레일 |
| 312p | 통과 | **확인 권장** | 에이전츠 애즈 툴즈 |

전 청크 발음 거리가 0.041 이하로 내려갔고, 남은 지적은 전부 **용어 이름과 함께**
나온다. 오탐 하나가 사라지고 미탐 셋이 드러났다.

### 자동으로 잡을 수 없는 것

`과드레일` vs `가드레일`은 자모 9개 중 1개 차이라 거리 0.111이다. 지정 발음
관문(0.15)보다 낮다. 이걸 잡을 만큼 임계치를 내리면 148페이지의 ASR 표기차가
다시 실패한다. **임계치는 건드리지 않았다.** 짧은 외래어의 모음 하나 차이는
이 계층이 약속할 수 있는 범위 밖이고, 영문 대신 한글을 합성 입력으로 주는
것이 실제 해결책이다.

### 기존 클립 380개 전수 판독 (재생성 없음)

`scripts/review_existing_clips.py`가 이미 만든 클립을 Whisper만 올려 다시
읽는다. 380개를 342초에 판독했다. 결과: **재생성 필요 38페이지, 확인 권장
59페이지.** 보고서는 `artifacts/reviews/2026-09-04/existing-clips.json`.

| 챕터 | 재생성 | 확인 |
| --- | --- | --- |
| ch00 | 0 | 3 |
| ch01 | 17 | 35 |
| ch02 | 22 | 31 |
| ch03 | 1 | 5 |

### 사전에 한글로 적어도 틀리게 읽힌다 — **§25에서 반증됨**

판독 직후에는 `래그`와 `런타임`이 처음부터 사전에 있던 항목인데도 계속 잘못
읽히므로 "한글로 바꾸는 것은 필요조건이지 충분조건이 아니다"라고 적었다.

```
래그   → 렉 · 랭 · 랜 · 렛 · 렐 · 랫      (약 25회, CH01 전반)
런타임 → 런트민 · 런틈 · 런팀 · 런트미 · 런티민  (11회, 9개 페이지)
```

**이 결론은 틀렸다.** §25의 생성 실험에서 두 용어 모두 모든 길이·위치에서
정확히 읽혔다. 위 소리들은 모델의 한계가 아니라 **검수 없이 한 번 만들고 끝낸
테이크**다. 소리 목록 자체는 사실이므로 남겨 둔다.

### 그 밖에 실제로 갈린 대목

```
봇을 → 보스를 (4)   임베딩 → 인베딩 (4)   스키마는 → 시키이미어는 (2)
디비 → 딥이 (2)     읽는 → 있는 (2)      일을 → 이를 (2)   결재를 → 결제를 (2)
```

ASR 표기차(그다음/그 다음, 한 번/한번, 십 년/10년, 케이 이삼일사/K 2314)는
발음 공간 비교가 이미 걸러낸다.

`안쪽입니다 → 아닙니다`가 2건 잡혔는데, 이건 오독이 아니라 이번에 고친 대본이다.
기존 음성은 `안입니다`라고 말하고 ASR은 `아닙니다`로 적었다 — 수정이 필요했다는
증거다.

### 아직 확인하지 않은 것

- 새 사전으로 CH03 L02를 **재생성**해 실제로 가드레일·에이전츠 애즈 툴즈가
  제대로 읽히는지. 위 표는 영문으로 만든 **기존** 음성을 다시 판독한 결과다.
- `래그`·`런타임`을 제대로 읽게 만드는 방법. 사전만으로는 안 된다.
- 148페이지 청취 확인(§22)은 여전히 남아 있다.

### 대본 자체의 애매함 (미결)

자막과 음성이 갈리므로 사전으로 덮을지 `udemy-agent` 대본을 고칠지 결정이 필요하다.

| 위치 | 대본 | 들린 소리 | 문제 |
| --- | --- | --- | --- |
| 304p | `환불 기한 안인가` | `아닌가` | 뜻이 정반대 |
| 98p·138p | `괄호 안입니다`·`네모 안입니다` | — | 같은 계열 |
| 310p 외 6곳 | `한 번의 실행에서` | `한 번에 실행해서` | 뜻이 바뀜 |
| 148p·176p | `네 기법을` | `내기법을` | 4개인지 소유격인지 |

### 테스트

Python 180개, 앱 55개. 새로 덮은 영역: 판독 창 분할(짧은 클립은 통으로,
무음에서 자르기, 무음이 없을 때, 빈 클립), 보호 구간(따옴표 영문 보존, 바깥
용어는 계속 치환, 보호 구간 안 숫자, 여러 구간의 복원 위치), 미지정 용어 경고.

## 24. 산출물 전체 삭제와 CH01 재촬영 (2026-09-04)

사용자가 지금까지의 생성물을 전부 지우고 CH01부터 다시 만들기로 했다.
`output/` 6.4GB(edits·projects·tts·videos·voices)를 삭제했다. 전부
`.gitignore` 대상이고 git이 추적하지 않던 생성물이다.

**지우지 않은 것**은 다시 만들 수 없거나 정본인 것들이다.

| 경로 | 크기 | 이유 |
| --- | --- | --- |
| `data/private/voice/` | 688M | 불변 정본 (AGENTS.md) |
| `artifacts/models/whisper-large-v3-turbo-asr-fp16` | 1.5G | 재다운로드 비용 |
| `artifacts/finetune-runs/2026-08-25/jaeho-ko-r16-v1/adapters` | 67M | 현재 제작 어댑터 |
| `artifacts/benchmarks/2026-08-23/reference.{wav,txt}` | 1.5M | 모든 생성의 참조 음성 |
| `artifacts/reviews/2026-09-04/existing-clips.json` | 184K | 삭제된 음성의 유일한 판독 기록 |

`artifacts/`도 `.gitignore` 대상이므로 판독 보고서는 git에 없다. 거기서 뽑은
결론은 §23에 적혀 있다.

### 인터프리터가 둘이다

`mlx_tune`은 `.venv`가 아니라 `.venv-train`에만 있다(DECISIONS.md 131행에서
추론 환경과 분리하기로 한 결정). 따라서 **LoRA 어댑터를 쓰는 실행은
`.venv-train/bin/python`이어야 한다.** 앱은 이미
`TTS_STUDIO_BASE_PYTHON`/`TTS_STUDIO_TRAIN_PYTHON`으로 나눠 쓰고 있다
(`electron-app/runtime-config.mjs`). 어댑터 없는 판독·테스트는 `.venv`로 된다.

### 다시 찍기 전에 풀어야 할 것

§23이 밝힌 대로 `래그`와 `런타임`은 사전에 한글로 적혀 있는데도 틀리게 읽힌다.
그대로 다시 찍으면 같은 소리가 난다. `scripts/probe_term_pronunciation.py`가
한 용어의 여러 표기를 실제 문장 안에서 생성하고 되읽어 어느 표기가 목표 소리로
읽히는지 측정한다. 제작에는 아무것도 바꾸지 않고 후보 클립과 보고서만 남긴다.

## 25. 오독의 원인은 사전이 아니라 검수 없는 단일 테이크였다 (2026-09-04)

§23이 "한글로 적어도 틀리게 읽는다"고 결론 낸 것을 생성 실험으로 반증했다.
`scripts/probe_term_pronunciation.py`가 실제 강의 문장 안에서 후보를 만들고
독립 ASR로 되읽어 목표 소리와의 거리를 잰다.

### 표기 실험 (6용어 × 후보 24개 × 3시드)

| 표기 | 들린 소리 | 최악 거리 |
| --- | --- | --- |
| `런타임` | 런타임 · 런타임 · 런타임 | **0.000** |
| `가드레일` | 가드레일 · 가드레일 · 가드레일 | **0.000** |
| `스키마` | 스키마 · 스키마 · 스키마 | **0.000** |
| `에이전츠 애즈 툴즈` | 에이전츠 에즈 툴즈 | 0.111 |
| `Guardrail` (영문) | 과드레일 · 가드레일 · 가우드레일 | 0.222 |
| `Agents-as-Tools` (영문) | 에이젠스 H-Tools · 에이젠스 에이스툴즈 | **0.500** |

제작에서 런팀으로 읽히던 바로 그 `런타임`이 세 시드 모두 정확했다. **사전은
제대로 작동하고, 영문을 그대로 넘기는 것이 실제로 나쁘다.**

곁가지 둘. `래그`는 실제로 `레그`로 읽히는데 ㅐ/ㅔ 합류라 귀로는 같은 소리다
(발음 공간이 이미 접는다). 제작에서 문제였던 건 `렉`·`랭`처럼 음절이 사라진
경우다. 그리고 `랙` 같은 1음절 표기는 랭/렉으로 무너지므로 쓰지 않는다.

### 길이 가설도 기각 (2용어 × 길이 4단 × 위치 2 × 3시드 = 42회)

제작 청크가 230~300자라서 길이가 원인일까 의심했다. 같은 용어를 81~337자
지문에 넣고 앞쪽·뒤쪽 양쪽에 두어 길이와 위치를 분리했다. **42회 전부 거리
0.000.** `MAX_CHUNK_CHARS`를 낮출 이유가 없다.

### 트림도 아니다

제작 클립은 `trim_and_fade_audio`를 거치고 실험 클립은 거치지 않는다는 차이를
확인했다. 클립 114개에 트림을 적용해 다시 읽혔더니 받아쓰기가 달라진 것은
49개지만 **용어 거리가 나빠진 것은 2개뿐**이고 둘 다 이미 나쁜 표기
(`라그`, `런타암`)였다. `런타임`·`가드레일`은 트림 뒤에도 0.000이다.

### 진짜 원인

판독 지적 125건 중 **119건이 검수 계층이 꺼진 회차**(`quality.enabled` 없음)에서
나왔다. 검수가 켜진 회차의 6건은 전부 입력이 아직 영문이던 CH03 L02 페이지다.

`automatic_quality`가 꺼져 있으면 `resolve_chunk_take(attempt_limit=1)`이다.
**청크마다 한 시드로 한 번 만들고, 되읽지 않고, 그대로 내보낸다.** 나쁜 테이크를
막을 것이 아무것도 없었다.

검수가 켜져 있었다면 그 소리들은 전부 걸렸다.

| 들린 소리 | 거리 | 판정 |
| --- | --- | --- |
| 런팀 | 0.250 | 확인 권장 → 다른 시드로 재시도 |
| 런트민 · 런틈 · 런트미 | 0.375 | 재생성 필요 |
| 런티민 | 0.250 | 확인 권장 → 다른 시드로 재시도 |

### 그래서 CH01 재촬영은 무엇을 바꾸는가

바꿀 것이 없다. 지금 파이프라인이면 된다.

- 발음 사전이 영문을 없앤다 (CH01~03 미지정 0건).
- 조기 종료 검수가 나쁜 테이크를 되읽고 다른 시드로 다시 만든다.
- 판독 창 교정이 30초 넘는 청크의 오탐을 없앤다.

`--no-auto-quality`로 끄지만 않으면 된다. **1~252페이지가 그렇게 만들어졌다.**

## 26. CH01 전체 재생성 (2026-09-04)

§25의 결론("바꿀 것 없이 지금 파이프라인이면 된다")을 실물로 확인했다.
전체를 지운 뒤 CH01 아홉 레슨을 CLI로 다시 만들었다.

| 레슨 | 페이지 | 청크 | 첫 시드 통과 | 확인 | 재생성 | 분 | 최대 GB |
| --- | --- | --- | --- | --- | --- | --- | --- |
| l01 | 12~30 | 27 | 26 | 0 | 1 | 10.1 | 12.3 |
| l02 | 31~58 | 38 | 34 | 0 | 4 | 16.7 | 12.6 |
| l03 | 59~65 | 9 | 8 | 0 | 1 | 3.3 | 12.3 |
| l04 | 66~74 | 13 | 12 | 0 | 1 | 4.6 | 12.3 |
| l05 | 75~86 | 17 | 17 | 0 | 0 | 6.1 | 12.5 |
| l06 | 87~99 | 17 | 16 | 0 | 1 | 5.4 | 12.3 |
| l07 | 100~107 | 9 | 9 | 0 | 0 | 3.1 | 11.6 |
| l08 | 108~119 | 22 | 20 | 1 | 1 | 9.6 | 12.5 |
| l09 | 120~128 | 11 | 11 | 0 | 0 | 4.0 | 12.3 |
| **계** | | **163** | **153 (94%)** | 1 | 9 | 62.9 | |

미지정 발음 0건. 최대 Metal 12.6GB. 생성 47분.

### 걸린 10건의 정체

| 페이지 | 걸린 것 | 청크 발음거리 | 판단 |
| --- | --- | --- | --- |
| 14p·41p | `맞혀` → 맞춰 | 0.006 · 0.009 | **진짜** (흔한 동음 혼동) |
| 111p | `노드가` | 0.015 | 진짜로 보임, 청취 |
| 61p | `오퍼스 파이브` → `오퍼스5` | 0.011 | 청취 필요 (ASR 숫자 정규화일 수 있음) |
| 36p | `지피티 포` → `GPT4` | 0.007 | **사전으로 해결** (아래) |
| 37p | `뱉을` → 배틀 | 0.003 | 오탐 (연음) |
| 53p | `세계가` → 세 개가 | 0.002 | 오탐 (`ㅖ`가 [ㅔ]) |
| 71p·98p | `사이의` → 사이에 | 0.003 | 오탐 (관형격 `의`가 [에]) |
| 116p | `건은` → 거는 | 0.011 | 오탐 (구어) |

청크 발음거리는 전부 0.015 이하다. 음성 자체는 깨끗하고, 걸린 것은 낱말 하나
수준이다.

### 사전으로 고친 것

사전은 대본과 받아쓰기 **양쪽**에 적용된다. Whisper가 `GPT-4`를 하이픈 없이
`GPT4`로 적으면 항목에 걸리지 않아, 정확히 읽은 음성이 불일치로 잡힌다.
`GPT4`·`GPT4o`를 항목으로 넣었다(36페이지 거리 0.250 → 0.000). 원문에는 없는
표기이므로 합성 입력은 달라지지 않고 판독 정규화에만 쓰인다.

### korean_phonetics를 고치지 않은 이유 (측정 근거)

오탐 넷은 전부 표준 한국어 발음 규칙이다. 연음(`뱉을`→[배틀]), 자음+`ㅖ`→[ㅔ],
조사 `의`→[에]. 발음 키가 이걸 접으면 사라진다. 특히 초성 `ㅇ`은 소리가 없으므로
빼는 것이 음운론적으로 옳다.

그런데 빼면 이렇게 된다.

| 비교 | 지금 | 초성 ㅇ 제거 후 |
| --- | --- | --- |
| `뱉을` vs `배틀` (오탐) | 0.167 | **0.000** |
| `런타임` vs `런팀` (진짜 오독) | 0.250 | **0.143** |
| `런타임` vs `런트민` (진짜 오독) | 0.375 | 0.286 |

일치 관문이 0.15이므로 **`런팀`이 관문 아래로 내려가 탐지 불가가 된다.** 이
저장소가 존재하게 만든 바로 그 증상이다. 오탐 하나를 없애려고 정탐 하나를
잃는 거래라 하지 않았다. `phonetic_variants`로 두 키를 다 제시해도 호출자가
최솟값을 쓰므로 결과는 같다.

즉 **음절이 사라지는 오독과 음절이 이어 붙는 정상 발음은 이 지표에서 같은
방향으로 움직인다.** 163청크에 오탐 네댓 건은 사람이 몇 초에 넘기는 비용이고,
`런팀`을 놓치는 쪽이 훨씬 비싸다.

### 청취 확인 목록

14p·41p(`맞혀`), 61p(`오퍼스 파이브`), 111p(`노드가`). 나머지 여섯은 위 표대로
오탐이거나 사전으로 해결됐다.

## 27. 관문을 더 조이지 않는다 (2026-09-04)

`n8n` 을 `엔팔엔` 으로 정한 뒤, ASR 이 `M8N` 으로 적어도 발음 거리가 0.111 이라
통과한다는 점을 "이 지표의 원리적 한계"라고 적었다. 사용자가 바로잡았다.

> 이정도는 사람이라면 당연히 이게 더 자연스러울 수도 있어.
> 오히려 너무 정확한 발음이면 AI 슬롭 냄새 날걸?

맞다. 첫소리가 살짝 무르는 것은 사람이 실제로 그렇게 말한다. LoRA 를 1.00 에서
0.60 으로 내린 이유도 같다 — 과한 AI 특성을 빼기 위해서였다. 그래 놓고 그
관용을 결함이라고 부르면 앞뒤가 맞지 않는다.

실제로 이 지표는 이렇게 갈린다.

| 차이의 종류 | 거리 | 판정 | 정체 |
| --- | --- | --- | --- |
| 자모 하나 (`엔팔엔`/`엠팔엔`, `가드레일`/`과드레일`) | 0.09~0.11 | 통과 | 사람 말투 |
| 연음·`ㅐ`/`ㅔ`·자음+`ㅖ`→[ㅔ] (`뱉을`→[배틀]) | 0.17 | 경고 | 표준 발음 |
| 음절이 사라짐 (`런타임`→`런팀`) | 0.25 | 경고→재시도 | 오독 |
| 다른 낱말 (`런타임`→`런트민`) | 0.38 | 재생성 | 오독 |

**관문은 "소리가 무른 것"은 통과시키고 "음절이 없어진 것"은 잡는 자리에 서 있다.**
`PRONUNCIATION_MATCH_DISTANCE = 0.15` 는 우연히 그 사이에 있는 것이 아니라 그
자리가 맞다.

### 그래서 하지 말 것

- **오탐을 줄이겠다고 관문을 조이지 않는다.** 0.09~0.11 을 잡으려고 내리면
  자연스러운 발화가 전부 재생성 대상이 되고, 148페이지의 ASR 표기차도 함께
  돌아온다.
- **오탐을 줄이겠다고 발음 키에서 연음을 접지 않는다.** §26 의 측정대로
  `런팀` 이 0.250 에서 0.143 으로 내려가 관문 아래가 된다.
- 잡히지 않는 자모 하나짜리 차이는 **사전의 표기를 바꿔서** 다룬다. `엔에잇엔`
  의 오독은 0.091 이라 안 잡혔지만 `엔팔엔` 에서는 같은 오독이 0.444 다.
  읽기 좋은 표기가 감시도 쉽게 만든다.

### 새 용어를 사전에 넣을 때 볼 것

읽는 법만 정하지 말고 **그 표기가 잘못 읽혔을 때 거리가 얼마나 벌어지는지**를
같이 본다. 음절이 적고 소리가 또렷한 표기일수록 오독이 크게 벌어져 검수가
쉬워진다. 긴 음차(`엔에잇엔`, 11자모)는 한 음절이 어긋나도 비율이 작아
사각지대를 만든다.

## 28. 강의 전체 사전 완성과 그 과정에서 드러난 구조 결함 (2026-09-04)

CH04~CH07 에 남아 있던 87건 34종을 사전에 넣었다. **여덟 챕터 전부 미지정 영문
0건.** 항목 243개.

그런데 이 작업이 두 가지 구조 결함을 드러냈고, 둘 다 §23~§27 에서 고친 것보다
조용한 부류였다.

### 긴 복합어 항목이 더 예민한 검사를 꺼 버린다

`에이전트 런타임` 같은 복합어를 사전에 넣으면 두 가지가 동시에 일어난다.

1. 지정 발음이 17자모가 되어, 음절 하나가 사라져도 0.118 로 관문을 통과한다.
2. `lexical_pronunciation_checks` 가 그 구성 낱말을 **제외 목록에 넣는다.**
   `런타임` 을 따로 보면 0.200 으로 경고가 뜨는데, 그 검사가 꺼진다.

즉 용어를 보호하려고 넣은 항목이 그 용어의 마지막 감시를 없앴다. 오늘 복합어를
대량으로 추가하면서 스스로 만든 구멍이다.

제외는 **한 낱말짜리 지정 발음일 때만** 하도록 바꿨다. 그때만 두 검사가 같은
것을 보고 판정이 중복되기 때문이다. 여러 낱말이면 지정 발음은 전체를, 어휘
관문은 낱말을 본다.

실측: CH01 163청크를 새 규칙으로 다시 채점했을 때 **새로 걸린 것 0건.** 구멍만
막히고 소음은 늘지 않았다.

### 하이픈으로 이어진 영숫자는 한 덩어리다

낱글자 `A`·`B`·`O`·`X` 를 넣자 `test_an_unmapped_letter_number_identifier_stops_before_generation`
이 깨졌다. `B-3099` 에서 `B` 만 떼어 가는 바람에 `MIXED_IDENTIFIER_PATTERN` 이
식별자를 못 보고, **`A-2041` 을 에이 이천사십일로 읽지 못하게 막는 관문이
무장해제됐다.**

`_dictionary_pattern` 의 경계를 고쳤다. 하이픈 양쪽이 영숫자면 경계가 아니다.

- `B-3099` 에서 `B` 는 매치되지 않는다.
- `Sub-Agent` 에서 `Agent` 도 매치되지 않는다 — §23 에서 복합어 항목을 넣어
  우회했던 것을 이제 구조가 막는다. 미지정 복합어는 `unresolvedAscii` 로 드러난다.
- 문장부호는 이음이 아니다. `Agent.` 는 계속 매치된다.

`BARE_NUMBER_PATTERN` 도 같은 문제였다. 사전이 `B` 를 안 건드려도 숫자 층이
뒤이어 `3099` 를 읽어 `B-삼천구십구` 가 됐다. 하이픈 **반대편에 글자가 있을
때만** 막는다. `2020-2024` 같은 순수 숫자 범위는 계속 읽는다 — §21 의 "숫자로
남은 숫자는 모델이 마음대로 읽는다" 가 그대로 적용되는 자리다.

### 사각지대 점검 도구

`scripts/check_dictionary_blind_spots.py` 는 각 항목의 읽기에서 **음절 하나가
사라졌을 때** 거리가 얼마나 벌어지는지 잰다. 관문 아래면 목록에 실린다.
현재 234개 중 42개다.

처음에는 모음 하나 바뀐 경우도 함께 쟀는데 243개 중 166개가 걸렸다. §27 이
정한 대로 모음 하나는 사람 말투이고 통과가 맞으므로, 그걸 세면 거의 모든
항목이 걸려 아무것도 알려주지 못한다. 음절 소실만 잰다.

목록에 올랐다고 고칠 것은 아니다. `에이전트 런타임` 은 그렇게 읽는 것이 맞다.
소리가 같은 값이면 짧은 쪽이 감시하기도 쉽다는 것만 알면 된다.

## 29. CH03 L02 영상 검증과 세션 마무리 (2026-09-04)

사용자가 앱으로 CH03 L02 를 영상까지 뽑아 확인했다. **오늘 바꾼 것 전부가
실물로 검증됐다.**

| 확인된 것 | |
| --- | --- |
| 가드레일 · 에이전츠 애즈 툴즈 | 영문 대신 한글을 합성 입력으로 주니 제대로 읽힌다 |
| 304p 0:01~0:32 | 오탐 사라짐 — 이 오탐이 이 세션의 출발점이었다 |
| 자막 | 원문 유지, 음성만 한글. 243항목을 넣은 뒤에도 계약이 지켜진다 |
| 영상 경로 | 촬영·자막·타임라인·발행까지 오늘 변경분으로 통과 |

사용자 평: "나머지는 그냥 완벽 그자체야 바로 그냥 출시해도 되겠는데".

### 남은 지적 하나와 그 처리

`한 끗이` 를 연음 없이 음절대로 읽는다. `똑같이 → 똑까치` 와 같은 부류여서
`한 끗이 → 한 끄시` 를 사전에 넣었다(표준 연음, 사용자 지정). 강의 전체에
306페이지 한 군데뿐이다.

**이미 발행한 CH03 L02 영상은 다시 뽑지 않기로 했다.** 따라서 그 영상의
306페이지 음성은 사전보다 한 세대 앞선다. 다음에 그 레슨을 다시 렌더하면
자동으로 맞춰진다. 사전과 발행물이 어긋난 유일한 지점이므로 여기 적어 둔다.

### 이 세션이 실제로 고친 것

시작은 "Agents-as-Tools 를 알아들을 수 없게 읽는다" 였다. 파고 보니 원인이
둘이었고, 큰 쪽은 발음이 아니었다.

1. **발음 사전에 영문이 그대로 남아 있었다.** CH01~CH03 에 123종 436건.
   여덟 챕터 전부 0건으로 만들었다(항목 244개).
2. **1~252페이지가 검수 없이 한 번 찍고 끝난 결과물이었다.** 판독 지적 125건
   중 119건이 검수 꺼진 회차에서 나왔다. `런팀` 도 여기서 나왔다. 사용자가
   전체를 지우고 다시 찍기로 한 판단이 결과적으로 정확했다.

그 밖에 판독 창(30초), 어휘 관문과 지정 발음의 상호작용, 하이픈 토큰 원자성,
영문 낭독 보호를 고쳤다. 관문 수치는 하나도 건드리지 않았다 — §27 참조.

### 다음 세션이 이어받을 것

- **CH00~CH02 렌더** (290장, 오디오 151분, 생성 112분 + 촬영). 레슨 단위로
  끊어야 영상이 편마다 나온다. 앱은 `--no-cache` 라 매번 새로 만든다.
- **CH03 은 대본 확정 후.** 사용자 기준 현재 90% 완성도.
- **udemy-agent 대본 6줄이 커밋되지 않았다** (§26 의 애매함 수정). 그 저장소에
  사용자의 다른 작업이 진행 중이라 손대지 않았다.
- CH04~CH07 은 사전만 채워 두었고 아직 한 번도 생성하지 않았다.

## 30. 제작 범위 선택 개편 (2026-09-04)

새 영상 화면의 제작 범위를 세 가지로 정리했다.

- `레슨`: 기존처럼 레슨 하나를 한 편의 영상으로 제작한다.
- `페이지 직접 선택`: 시작 페이지부터 끝 페이지까지의 범위를 제작한다.
- `챕터 전체`: 챕터 전체를 `한 영상으로 제작`하거나 `레슨 단위로 나눠 제작`한다.

기존 `30초 미리보기`와 페이지 선택 안에 있던 `챕터 이동`·`챕터 전체` 버튼은
제거했다. 챕터의 레슨 단위 제작은 레슨 카탈로그의 각 범위를 순서대로 실행해
결과 영상들을 독립 파일로 저장하며, 각 파일은 `최근 결과`에 따로 표시된다.

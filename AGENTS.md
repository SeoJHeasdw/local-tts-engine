# Local TTS Engine 작업 지침

## 임무

이 저장소는 MacBook M4 Max 36GB에서 한국어 강의용 개인 음성을 로컬로
생성하는 재사용 가능한 TTS 엔진이다. 첫 소비자는
`/Users/jaehoseo/Desktop/vswrk/edu/udemy-agent`이다.

## 새 작업이 가장 먼저 할 일

1. `docs/HANDOFF.md`를 끝까지 읽는다.
2. `docs/ARCHITECTURE.md`와 `docs/BENCHMARK-PLAN.md`를 읽는다.
3. `python3.13 scripts/check_context.py`로 외부 정본 경로를 확인한다.
4. 사용자가 별도로 방향을 바꾸지 않았다면 Qwen3-TTS와 Chatterbox의 짧은
   A/B 파일럿부터 진행한다.

## 확정된 결정

- Python 런타임은 사용자가 이미 쓰는 3.13.12로 고정한다.
- ElevenLabs를 기본 경로로 사용하거나 결제하지 않는다.
- Qwen3-TTS 1.7B Base + MLX-Audio를 1순위로 검증한다.
- Chatterbox Multilingual V3 + MLX-Audio를 같은 조건의 비교군으로 둔다.
- 53분 음성 전체를 바로 미세조정하지 않는다. 먼저 10~20초의 깨끗한 참조
  음성으로 zero-shot 복제를 시험한다.
- zero-shot의 한국어 품질과 장문 안정성이 충분하되 닮은 정도만 부족할 때만
  미세조정을 검토한다.
- 최종 모델 선택 전에는 기존 영상 파이프라인을 대규모로 수정하지 않는다.

## 데이터와 안전

- `udemy-agent/deck/voice/`의 원본과 정제본은 불변 정본이다. 덮어쓰거나
  재인코딩하지 않는다.
- 실험 입력은 복사본이나 읽기 전용 참조로 사용한다.
- 모델 가중치, 음성 원본, 생성물, 캐시, `.env`, 토큰은 Git에 커밋하지 않는다.
- API 키, 로그인 세션, ElevenLabs 비밀값을 요청하거나 문서에 기록하지 않는다.
- 상업용 강의이므로 모델·음성·코드의 라이선스를 선택 시점에 다시 검증하고
  `docs/DECISIONS.md`에 출처와 날짜를 남긴다.

## 품질 기준

- 같은 원문·참조 음성·출력 포맷으로 모델을 비교한다.
- 목소리 유사도, 한국어 발음, 영어 약어, 억양, 반복/누락, 장문 안정성,
  생성 속도와 최대 메모리를 기록한다.
- TTS용 발음 치환문과 자막용 원문을 분리한다.
- 긴 대본은 의미 단위의 짧은 클립으로 생성하고 해시 캐시한다.
- 출력 길이는 추정하지 말고 실제 오디오에서 측정한다.
- 최종 판단은 수치만으로 하지 않고 사용자의 청취 승인을 받는다.

## 연동 규칙

- 기존 `deck/tools/narration.mjs`의 클립·해시·타임라인 계약을 보존한다.
- 로컬 엔진은 먼저 독립 CLI로 만들고, 안정화 후 로컬 HTTP API 또는 얇은
  Node 어댑터를 추가한다.
- 강의 영상, 자막, 슬라이드 렌더 코드는 `udemy-agent`에 남긴다.
- 기존 저장소를 수정할 때는 그 저장소의 `NARRATION-PIPELINE.md`와
  `VIDEO-PACING-GUIDELINES.md`를 먼저 읽는다.

## 사용자와의 협업

- 사용자에게는 한국어로, 결론부터 간결하게 보고한다.
- 설치와 모델 다운로드 전에 예상 용량과 작업 목적을 알린다.
- 한 번에 전체 7~8시간을 생성하지 않는다. 30~60초, CH01 일부, CH01 전체,
  전체 강의 순으로 승인받아 확장한다.
